<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.goods.dao.GoodsMapper">

	<select id="getASList" parameterType="map"
		resultType="goodsCommand">
		SELECT	*
		FROM as_goods
	<where>
			<if test="keyword != '' and keyfield == 'as_name'">
				as_name like '%' || #{keyword} || '%'
			</if>
			<if test="keyword != '' and keyfield == 'as_location'">
				as_location like '%' || #{keyword} || '%'
			</if>
			<if test="keyword != '' and keyfield == 'all'">
				as_name like '%' || #{keyword} || '%' or
				as_location like '%' || #{keyword} || '%'
			</if>
		</where>
ORDER BY as_id desc
 	<!-- <![CDATA[
 		WHERE rnum >= #{start} AND rnum <= #{end}
 	]]> -->
	</select>

	<select id="selectRowCount" parameterType="map"
		resultType="integer">
		SELECT
		count(*)
		FROM as_goods
		<where>
			<if test="keyword != '' and keyfield == 'as_name'">
				as_name like '%' || #{keyword} || '%'
			</if>
			<if test="keyword != '' and keyfield == 'as_location'">
				as_location like '%' || #{keyword} || '%'
			</if>
			<if test="keyword != '' and keyfield == 'all'">
				as_name like '%' || #{keyword} || '%' or
				as_location like '%' || #{keyword} || '%'
			</if>
		</where>
	</select>

</mapper>







